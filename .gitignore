# Dependencies
node_modules/

# Build outputs
.next/
**/next-env.d.ts
dist/
out/
# TypeScript incremental-build cache. AGENTS.md already listed *.tsbuildinfo as
# generated content, but it was never ignored, so apps/web/tsconfig.tsbuildinfo
# sat in git for two commits and rewrote itself on every local `tsc`.
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
.env.runtime
*.runtime

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Backup / vestigial scratch files
**/*.bak
**/*.bak.*
ersahazan2Desktopsynology-monitor

# Turbo
.turbo/

# Go
apps/agent/synology-monitor-agent
apps/agent/data/

# Supabase
supabase/.temp/

# Docker
apps/agent/data/
.claude/**
!.claude/settings.json
package-lock.json
!apps/nas-mcp/package-lock.json

# MCP client config — holds the nas-mcp bearer token in plaintext. It was only
# ever covered by .git/info/exclude, which is local to one clone and does not
# travel; a fresh clone would have happily committed the token. That already
# happened once: 006c059 committed a live token in ROO_MCP_GUIDE.md and 11f5d5e
# removed the file, but the value stayed in history and had to be rotated.
.mcp.json
